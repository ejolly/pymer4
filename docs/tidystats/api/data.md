# `tidystats._data`

The `data` module includes a single function to load a variety of datasets included with `pymer4`. Datasets are always returns as `polars` DataFrames.

::: pymer4.tidystats._data
    options:
      show_root_toc_entry: false
      show_symbol_type_toc: True
      heading_level: 4